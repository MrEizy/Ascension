TODO:
    setup gather types and damage event
    setup event triggers
    start working on ascension implementation alongside it

TODO:
    figure out more about generics, and if they can be used with data source+instance to guarantee the type

TODO:
    for ascension implementation work with an AscensionOriginSource, then do the Server ver as well
figure out how i want to unify the damage system. especially considering that because of how different they are, the order is important

so potentially create a custom damage pipeline? then add multiple ways for mods to access and modify it at different points

so this would look like

pre damage.
here is where we need to figure out WHAT damage was dealt(for ascension this would be path damage, and for other mods this might include other things)
so create like an RPGEngineSource, and other mods can attach containers to this source

damage.
this is where the damage formula is actually handled.

in my opinion it would be best to limit the "direct" influence of other mods on this formula directly, instead giving them access to multiplier buckets to add damage modifiers

(basically they can only modify it through the value container modifiers)
