think about how I want to handle data pack stuff


we will basically have a Type for each Data Packable registry

this Type tells us how to map a json file -> a registry object



when a Origin source is "split" in order to properly reconstruct it, instead of relying on complex event chaining
we should instead "save" the cultivation the "re-load" it essentially simulate it
OR i just do remove from old source and add to new source


i need to consider how stuff like skills are handled.
because they are stored on the source not the player they do not have direct access to ALL entities listening to a source
when added

there are 2 solutions to this.
1. passive skills should not be able to give entity specific effects ON_ADDED, but should still be able to by listening to events
2. each entity listens to the SkilLAdded event and calls skill.addedToEntity


Active skills are no problem since they are given a direct entity reference

