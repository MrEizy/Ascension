priority:

origin source
SimpleEntityData Wrapper
PathData (initial)
Technique (initial)
PathData (finish)
Technique (finish)



TODO set up the tooltip thing eizy wanted
smth like
item_tooltip : [
    "title":[
        "ascension:technique_name"
    ],
    "1":[
        "ascension:technique_description"
    ]
]

the tooltips then receives a "context" that holds an identifier
TODO create a global server registry access?


create a dashboard.
so write a mod that can hook into the server endpoints and read certain data from it
this would then be displayable on a (local) browser window, with realtime updates